<TdComponent TextAlignment="MR">@AfsConstraints.RoundStr(Item.BaseYear, Digits)</TdComponent>
<TdComponent TextAlignment="MR">@AfsConstraints.RoundStr(Item.CurrentYear, Digits)</TdComponent>
<TdComponent TextAlignment="MR">@AfsConstraints.RoundStr(Item.Deviations, Digits)</TdComponent>
<TdComponent TextAlignment="MR">@AfsConstraints.RoundStr(Item.GrowthRate, Digits)</TdComponent>

@code {
    [Parameter]
    public TwoYearsCalculationData Item { get; set; } = new();

    [Parameter]
    public int Digits { get; set; } = 1;
}
