<tr>
    <TdComponent TextAlignment="ML">@Item.Number</TdComponent>
    <TdComponent TextAlignment="ML">@ChildContent</TdComponent>
    <TdComponent TextAlignment="MR" Spacing="pr-2">@AfsConstraints.RoundStr(Item.BaseBegin,Digits)</TdComponent>
    <TdComponent TextAlignment="MR" Spacing="pr-2">@AfsConstraints.RoundStr(Item.BaseEnd,Digits)</TdComponent>
    <TdComponent TextAlignment="MR" Spacing="pr-2">@AfsConstraints.RoundStr(Item.CurrentBegin,Digits)</TdComponent>
    <TdComponent TextAlignment="MR" Spacing="pr-2">@AfsConstraints.RoundStr(Item.CurrentEnd,Digits)</TdComponent>
    <TdComponent TextAlignment="MR" Spacing="pr-2">@AfsConstraints.RoundStr(Item.BaseDeviation,Digits)</TdComponent>
    <TdComponent TextAlignment="MR" Spacing="pr-2">@AfsConstraints.RoundStr(Item.CurrentDeviation,Digits)</TdComponent>
</tr>

@code {
    [Parameter]
    public SolvencyRatiosRow Item { get; set; } = new();
    [Parameter]
    public int Digits { get; set; } = 3;
    [Parameter]
    public string Number { get; set; } = string.Empty;
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
